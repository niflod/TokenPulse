services:
  ai-monitor:
    build: .
    container_name: ai-usage-dashboard
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/backend/data
    environment:
      - HOST=0.0.0.0
      - PORT=8000
