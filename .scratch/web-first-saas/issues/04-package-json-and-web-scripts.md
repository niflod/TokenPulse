# 04: Configuração de package.json e Scripts Web

**What to build:**
Adicionar um arquivo `package.json` na raiz do repositório configurado para projetos web, além de modernizar `start.sh` para refletir um servidor web amigável:
1. `package.json`: metadados, scripts `dev`, `start`, `test`, `build`.
2. `start.sh`: output estilizado mostrando URLs do navegador:
   - Site / Landing Page: `http://localhost:8000`
   - Dashboard: `http://localhost:8000/dashboard`

**Blocked by:** 03

**Status:** completed

- [x] Criar `package.json` com scripts do ecossistema web.
- [x] Atualizar `start.sh` para exibir links web formatados e claros.
